package com.level;

/**
 * All the different highlightings that a field can have
 *
 */
public enum Highlighting {
    Empty,
    Attackable,
    Movable,
    MovableUp,
    MovableDown,
    MovableRight,
    MovableLeft,
    Reachable,
    Healable,
    Buildable
}

